"use strict"
let Caf = require('caffeine-script-runtime');
Caf.defMod(module, () => {return Caf.importInvoke(["BaseClass", "Error", "formattedInspect"], [global, require('../StandardImport')], (BaseClass, Error, formattedInspect) => {let LocalFs; return LocalFs = Caf.defClass(class LocalFs extends BaseClass {constructor(prefix) {super(...arguments); this.files = [];};}, function(LocalFs, classSuper, instanceSuper) {this.property("files"); this.prototype.getFirstIndexAfter = function(key) {let files, i, j; files = this.files; return (files.length === 0 || key < files[0]) ? 0 : (i = 0, j = files.length, (() => {while (i + 1 < j) {let m; m = (i + j) / 2 | 0; if (key < files[m]) {j = m;} else {i = m;};};})(), j);}; this.list = ({bucket, prefix, limit = 1000, startAfter, localFs}) => {throw new Error(`Not expecting a value for \`bucket\` when listing from the local file system. Got: ${Caf.toString(formattedInspect({bucket}))}`); return startAfter != null ? startAfter : startAfter = prefix;};});});});
//# sourceMappingURL=LocalFs.js.map
